<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.niit.cmsdemo.dao.UserDao">
    <resultMap id="BaseResultMap" type="com.niit.cmsdemo.domain.User">
        <id column="login_id" property="login_id" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="DATE"/>
        <result column="update_time" property="updateTime" jdbcType="DATE"/>
    </resultMap>
    <insert id="insertOne" parameterType="com.niit.cmsdemo.domain.User">
        INSERT INTO USER VALUES(#{login_id},#{username},#{password},now(),now())
    </insert>
    <delete id="deleteOne" parameterType="java.lang.String">
        DELETE FROM USER WHERE id = #{id}
    </delete>
    <update id="updateOne" parameterType="com.niit.cmsdemo.domain.User">
        UPDATE USER SET
        login_id = #{login_id},
        username = #{username},
        password = #{password},
        create_time = now(),
        update_time = now()
    </update>
    <select id="selectAll"  resultMap="BaseResultMap">
        SELECT * FROM USER
    </select>
    <select id="selectAll" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT * FROM USER WHERE id = #{id}
    </select>
</mapper>